# 精确依赖锁定（Linux/CUDA 评测环境）
# 提交代码 import：torch / torchvision / numpy / PIL / onnx / onnxruntime
#   （onnx/onnxruntime 仅 predict 端 ONNX 加速路径 import；缺省时自动回退 PyTorch 主干）。
# torch / torchvision 由评测平台按 CUDA 环境安装，此处为代码验证版本（本机 pip freeze 的 +cpu 后缀仅限 Windows 本地，已去除）。
# 如需重新生成：在评测等价环境执行 python -m pip freeze 后仅保留以下条目。
torch==2.12.0
torchvision==0.27.0
numpy==2.4.6
pillow==11.3.0
onnx==1.22.0
onnxscript==0.7.1     # torch>=2.12 dynamo 导出器翻译图所需（仅 train 端 export 需要）
onnxruntime==1.29.0   # CUDA 环境如需 GPU 后端请改用 onnxruntime-gpu（版本一致）

