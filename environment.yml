# conda 环境定义 —— 两台机器统一用 Python 3.12
# 用法：conda env create -f environment.yml && conda activate iad
# 注意：torch 仍按平台单独装（见 README），这里只锁定 Python 版本 + 其余依赖
name: iad
channels:
  - conda-forge
  - pytorch
dependencies:
  - python=3.12
  - pip
  - pip:
      - -r requirements.txt
